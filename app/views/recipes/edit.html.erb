<h3> Make Changes to your recipe </h3>

<%= form_for @recipes do |f| %> 
    <%= f.label :title %>
    <%= f.text_label :title %>
    <br><br>
    <%= f.label :content %>
    <%= f.text_area :content %>
    <% f.submit %>
    <% end %> 