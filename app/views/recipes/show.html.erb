<h1 class="center", class="orange-text" > Recipe Details </h1>

<h4 class="center"><%= @recipe.title %> </h4>
<h4>Posted By: <%= link_to @recipe.user.username, user_path(@recipe.user)%></h4>
<h4 class="green-text">Here is what you'll need:</h4><ol><% @recipe.ingredients.each do |i| %><br>
  <li>  <%= i.name %><br> </li> 
    <% end %>
    </ol> </p>
<h4>Directions: <%= @recipe.content %></h4><br>



<%= link_to "See All Comments", recipe_comments_path(@recipe)%><br>
<%= link_to "Add a new comment", new_recipe_comment_path(@recipe)%><br>
<%= link_to "See all ingredients here", recipe_ingredient_path(@recipe)%>

<h4>Comments: 
<% @recipe.comments.each do |com| %>
    <%=com.user.username%> says: <br>"<%=link_to com.content, comment_path(com)%>"<br><br>
    <% end %> <br> </h4>

    
 
 