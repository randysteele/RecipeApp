<h1> Ingredient Details  </h1>

<%= @ingredient.name%>  
<%= @ingredient.quantity%>
<%= @ingredient.measurement%><br><br>
<%= link_to "Back to Recipe", recipe_path(@ingredient.recipe_id)%>

<%= form_tag ingredient_path(@ingredient.id), method: "delete" do %>
  <%= submit_tag "Delete #{@ingredient.name}" %>
<% end %>


